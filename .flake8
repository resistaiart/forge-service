[flake8]
# Maximum allowed line length
max-line-length = 100

# Ignore specific rules that conflict with Black formatting
extend-ignore = E203, W503

# Exclude common non-code folders
exclude = .git, __pycache__, .venv, env, dist, node_modules
