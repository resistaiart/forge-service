[flake8]
# Max line length (Black compatibility)
max-line-length = 100

# Ignore rules to align with Black formatting
extend-ignore = E203, W503

# Exclude common non-source folders and cache dirs
exclude = .git, __pycache__, .venv, venv, env, dist, node_modules
