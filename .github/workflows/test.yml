    strategy:
      matrix:
        python-version: [3.9, 3.10, 3.11]

    - name: Run tests with coverage
      run: |
        pip install pytest pytest-cov
        pytest --cov=forge --cov-report=xml
